type Query {
    user(id: ID!): User
    users: [User]!
    usersByChildIds(ids: [ID!]!): [User]!
}

type User {
    id: ID!,
    firstName: String,
    lastName: String,
    patronymic: String,
    email: String,
}